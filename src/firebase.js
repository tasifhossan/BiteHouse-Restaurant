// simple stub — fill with your firebase config if needed
// import { initializeApp } from "firebase/app";
// const firebaseConfig = { /* ... */ };
// const app = initializeApp(firebaseConfig);
// export default app;

export default null;
